# Contexto do projeto — barbearia-saas

Leia isto no início de qualquer sessão nova. Arquitetura detalhada está no [README.md](README.md) — aqui é o estado atual e as convenções operacionais que não estão lá.

## O que é

SaaS multi-tenant de agendamento para barbearias: painel web (dono/barbeiro) + bot de WhatsApp com IA (Anthropic) fazendo o autoatendimento do cliente final. Reescrita profissional (TS/Postgres/Prisma) de um protótipo anterior (`barbearia-bot`, outro diretório, mantido só como referência histórica — não misturar convenções).

**`odonto-saas`** (`C:\Users\Admin\Documents\Claude\odonto-saas`, github.com/iomeswesley/odonto-saas) é um **produto irmão** pro segmento odontológico: nasceu em 2026-08-03 como cópia mecânica deste projeto (`Barbershop`→`Clinic`, `Barber`→`Dentist`), mesma arquitetura, evoluem em paralelo. É comum portar feature/fix de um pro outro nos dois sentidos — ao investigar um bug de UI/UX aqui, vale checar se o mesmo padrão existe (ou já foi corrigido) lá, e vice-versa.

- Repo: github.com/iomeswesley/barber, branch `master` (sem branches de feature)
- Deploy: Vercel, produção em https://agenda-barb.vercel.app (`barbearia-saas-jet.vercel.app` era o domínio antigo — hoje só redireciona pra esse; **nunca cadastrar webhook do Stripe/Meta nele**, redirect 307 faz a entrega falhar silenciosamente, foi a causa de um pagamento não sincronizar em 2026-07-26)
- Banco: Postgres via Supabase (Prisma) — **um único banco, sem staging/teste separado**. Dev local aponta pro mesmo banco de produção.

## Convenções de frontend

O design system é estilo **Cal.com** desde 2026-08-15 (antes era "Google style" verde, adotado em 2026-08-02, que por sua vez substituiu "dark glassmorphism verde neon"): neutros mais quentes, marca preto/branco em vez de cor saturada, cantos mais fechados, fonte Inter auto-hospedada (`webroot/assets/fonts/*.woff2`). Portado do `odonto-saas` (que passou pelo mesmo redesign primeiro, apelidado de "Claude Design" lá) — `webroot/assets/theme.css`, `login.html`, `signup.html` e parte de `admin.html` (Configurações reorganizada em `.settings-section`, "Serviços" virou página própria na sidebar) já estão no padrão novo. Continua tudo superfície chapada, borda de 1px, sombra discreta de elevação, **tema claro como padrão**, escuro pelo toggle — nada de vidro (`backdrop-filter`), glow, gradiente de marca ou fundo decorativo animado.

- **`webroot/assets/theme.css` tem 3 camadas** — leia o cabeçalho do arquivo antes de mexer. Pra reskin (trocar verde por azul, etc.) mexe-se **só na camada 1 (paleta)**; a camada 2 (semântica) deriva dela via `var()` e a 3 (componentes) consome só a 2.
- **Toda cor da paleta é trinca RGB sem vírgula** (`32 33 36`), nunca hex — permite `rgb(var(--p-gray-900))` e `rgb(var(--p-gray-900) / 0.4)` da mesma entrada. **Não escrever hex/rgb literal fora da camada 1**, nem nos `<style>` inline das páginas (usar os tokens semânticos).
- **Toda página nova precisa de `<script src="/assets/theme-init.js"></script>` no `<head>`, sem `defer`, antes do `theme.css`** — é o que aplica o tema salvo antes da primeira pintura. Sem isso, quem usa tema escuro vê um flash branco a cada navegação. (`theme-toggle.js` continua `defer` e cuida só do clique no switch.)
- **Tons de texto e de gráfico são separados de propósito**: texto precisa de 4.5:1 (WCAG AA) e objeto gráfico se resolve com 3:1 — por isso `--brand` (botão/texto) é mais escuro que `--brand-chart` (barra/linha). Ao trocar a paleta, revalidar os pares texto/fundo.
- **Todo `<select>` novo em `webroot/*.html` precisa de `color-scheme: light dark` + fundo sólido (`var(--card-bg-solid)`) no próprio `select` e no `select option`** — sem isso o dropdown nativo abre com fundo branco no dark mode (o navegador ignora CSS de fundo transparente no popup nativo). O padrão certo já existe em `theme.css` (`.form-field select`), mas só se aplica a elementos com essa classe — não herda automaticamente pra selects fora desse padrão.
- **`chat.html` está fora do design system de propósito** — é um simulador da tela do WhatsApp (paleta verde/branco própria, mimetiza o app real pra testar o bot). Não aplicar os tokens lá.

## Convenções operacionais (importante seguir)

- **Antes de qualquer commit**: `npx tsc --noEmit` e `npx vitest run` têm que passar limpos.
- **Depois de qualquer mudança validada, dar `git push origin master` sem perguntar** — instrução explícita do usuário (2026-07-20): "sempre que fizer algo já manda pra nuvem". Vale para commits normais; ações destrutivas (force push, reset, etc.) continuam exigindo confirmação.
- **Depois do push**, o Vercel às vezes não promove o deployment novo pra produção sozinho — checar com `npx vercel ls`/`vercel inspect <url>` (`target: production` vs `preview`). **Descoberto em 09/09**: dá pra promover direto pela CLI autenticada nesta máquina (`npx vercel promote <url> --yes`, sem precisar do dashboard). **Atualizado em 10/09** — instrução explícita do usuário ("Sempre", ao ser perguntado se podia promover): pode promover sem perguntar de novo, desde que já validado que o build passou (`tsc`/`vitest` limpos, deploy com `status: Ready`). `vercel promote <preview-url>` faz um build novo direto como produção (não só re-alias do build existente) — esperar terminar (`vercel inspect <url> --wait` ou reconferir `vercel ls`) antes de considerar concluído, e conferir depois que o alias `agenda-barb.vercel.app` migrou pro deployment novo.
- **Migrations do Prisma**: nunca rodar `prisma migrate dev` direto — ele detecta "drift" por causa da tabela `session` (criada em runtime pelo `connect-pg-simple`, fora do controle de migrations do Prisma) e tenta oferecer um **reset completo do banco de produção**. Sempre criar a pasta de migration manualmente (copiando o padrão de `prisma/migrations/*/migration.sql`) e aplicar com `npx prisma migrate deploy`, que só aplica migrations pendentes sem checar drift.
- **Testes de integração** usam o banco real — sempre prefixar dados de teste com `[teste]` e limpar no `afterAll` (ver `src/modules/appointments/appointments.service.test.ts` como modelo). Nunca rodar ações reais (reset de senha, cobrança, envio de WhatsApp) contra contas de clientes de verdade — criar um registro `[teste]` descartável, validar, apagar.

## Status (última atualização: 2026-08-23)

Tudo abaixo já está implementado e em produção — o README pode estar desatualizado em relação a isso, checar o código antes de assumir que algo é "próximo passo":

### Motor único portado do odonto-saas — Fases 0 a 8 (2026-08-16)

O `odonto-saas` implementou primeiro um roadmap grande de 11 fases (motor único, Fases 0-8, + prontuário odontológico, Fases 9-10, essas últimas fora de escopo aqui). Todo o motor único foi portado manualmente pra cá na sequência (backend primeiro, frontend depois), cada fase com commit próprio, testado num dev server local contra o banco real de produção (dados `[teste]` limpos depois) antes de cada push:

- **Fases 0, 1** — `AppointmentStatus.scheduled` (agendado, ainda não confirmado) + `Appointment.confirmationToken`; lembrete automático (`src/jobs/reminders.ts`) manda link de confirmação (`GET /api/public/appointments/confirm` → `webroot/confirmar.html`).
- **Fase 2** — Botão "bloquear o dia todo" no cabeçalho de cada dia da Agenda (grade desktop + lista mobile, `admin.html`) e em `barber.html` (agenda do próprio barbeiro).
- **Fase 3** — Aba **Conversas → Mensagens**: layout de 3 colunas — lista com badge "precisa de atenção"/"IA respondeu" + prévia da última mensagem, chat central com botão de anexo (`sendManualAttachment`/`uploadWhatsappMedia`/`sendWhatsappMedia` em `src/lib/whatsapp.ts`), coluna de info do cliente com toggle **"IA Ativa"** que pausa resposta automática só naquela conversa (`ChatSession.aiPaused`, `setAiPaused` em `chatEngine.ts`).
- **Fase 4** — Nova aba **Campanhas**: reativação em massa via WhatsApp (`src/modules/campaigns/`), nunca manda campanha 2x pro mesmo cliente em 90 dias (`Campaign`/`CampaignSend`), respeita opt-in de marketing.
- **Fase 5** — Visão Geral dividida em **Atendimentos** (agendamentos de hoje + resumo de conversas do WhatsApp + avaliações) e **Métricas** (KPIs, gráfico de faturamento/ocupação, análise por barbeiro) — gráficos recarregam toda vez que a aba Métricas abre, sem gate de "já carregou" (evita canvas com tamanho zero criado com a aba ainda escondida). Ícone novo `icon-trending-up` em `assets/icons.svg`.
- **Fase 6** — Aba **Clientes** própria (antes vivia dentro de Histórico), com pills de filtro por status (Novo/Ativo/Inativo/Retorno, derivado de `dueStatus` + dias desde a última visita) e busca por nome ou telefone.
- **Fase 7** — Configurações: **Personalidade da IA** (4 presets fixos, injetados no system prompt antes dos exemplos de tom de voz) e **Prompt Mestre** (regras extras de texto livre, injetadas como reforço no fim do prompt, nunca podendo anular as regras de segurança — `Business.aiPersonality`/`masterPrompt`).
- **Fase 8a** — Google Agenda por barbeiro (OAuth2 via `src/lib/googleCalendar.ts`, fetch puro sem SDK `googleapis`) espelha agendamentos criados/cancelados/reagendados (fire-and-forget, nunca bloqueia o agendamento); import de calendário externo `.ics` (`src/lib/icsParser.ts` + `src/jobs/icalImport.ts`, piggyback no mesmo cron diário de lembretes) vira `TimeBlock` com `source: "ical_import"`.

Migrations aplicadas via `prisma migrate deploy` (nunca `migrate dev`, mesma cautela de sempre por causa da tabela `session`): `20260816300000_add_scheduled_appointment_status`, `20260816300100_motor_unico_fases_1a8`.

**Não portado de propósito**: convênios médicos e prontuário (Fases 9-10 do odonto-saas) — são features específicas de clínica odontológica, não fazem sentido pra barbearia.

**Env vars do Google Agenda configuradas em 20/08** — ver seção "Sessão de 20/08" abaixo pro que ainda falta (redirect_uri corrigido, testadores do OAuth pendentes).

**Retoques de UI pós-port (mesmo dia)**: hint da mensagem de campanha (`Nova campanha de reativação`) estava saindo grande/sem estilo porque a classe `.field-hint` nunca tinha CSS definido — trocado por `style` inline pequeno e mudo (11.5px, `--text-muted`); botão de salvar aniversário na tabela de clientes estava empilhando embaixo do campo de data — envolvido num `display:flex` pra ficar ao lado. Mesmo ajuste feito no odonto-saas.

### Sessão de troubleshooting pós-port (2026-08-18)

- **`VAPID_SUBJECT` configurado** — já existiam `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` em produção (não mexer nessas duas, existem inscrições de push já vinculadas a essa chave pública), só faltava o `mailto:` de contato exigido pelo protocolo Web Push. Confirmado via `GET /api/push/vapid-public-key` retornando a chave — push notifications funcionando ponta a ponta agora.
- **Corrigido bug de largura do card "Avaliações" em modo barbeiro-único** (mesmo bug do `odonto-saas`, herdado do port de 16/08): `applyBarberModeGating()` ainda tentava realocar `reviews-card` pra dentro de `.overview-grid-two` pra preencher a vaga que `barber-analysis-card` deixava ali — mas desde a Fase 5 (split Atendimentos/Métricas) esse card não mora mais nessa grade (foi pra Métricas), então a grade sempre tem os 2 slots ocupados (Agendamentos de hoje + Conversas do WhatsApp) e a realocação só espremia "Avaliações" pra metade da largura à toa. Removida a realocação.

- Onboarding self-service, LGPD (privacidade/termos/exclusão), Sentry, headers de segurança (helmet), recuperação de senha, cobrança via Stripe (Starter/Pro).
- WhatsApp Cloud API oficial da Meta (não Baileys) — `src/lib/whatsapp.ts` + `src/modules/whatsapp/`, com Message Templates aprovados pra lembrete/reagendamento/reconquista.
- Landing page de marketing na raiz do site, no design system "Google style" atual (ver Convenções de frontend) — não confundir com a descrição antiga "dark glassmorphism verde neon", que foi substituída em 2026-08-02.
- **Painel de super-admin da plataforma** (`/superadmin.html`): lista todos os usuários de todas as barbearias e permite gerar senha aleatória + mandar por e-mail. Login pela mesma tela/rota de dono/barbeiro (`/login.html` → `POST /api/auth/login`), que reconhece as credenciais fixas via env (`ADMIN_USERNAME`/`ADMIN_PASSWORD_HASH`, não uma conta na tabela `users`) antes de cair na busca normal. Ver `src/modules/superadmin/`.
- Auditoria de segurança rodada e corrigida: IDOR entre tenants no agendamento público (barbeiro/serviço de uma barbearia não podem mais ser usados pra criar agendamento em outra) e fail-closed no webhook do WhatsApp (antes aceitava requisição sem assinatura se `WHATSAPP_APP_SECRET` sumisse).
- **`admin.html` e `barber.html` convertidos pra menu lateral (sidebar)**, com off-canvas no mobile. `admin.html` também ganhou: topbar com busca global (Ctrl K, por cliente), sino de notificações (espelha os mesmos alertas do banner), menu de ajuda e avatar com popup de "Minha conta" (só nome + foto — `webroot/assets/profileModal.js`, coluna `User.avatarUrl`); KPIs em layout split (card de faturamento 2x2 + coluna estreita); e **modo automático barbeiro-único vs múltiplos** (`isSoloMode()`/`applyBarberModeGating()`): com 0 ou 1 barbeiro cadastrado, esconde faturamento líquido, comissão, "barbeiro do mês" e "análise por barbeiro" em toda a UI (Visão Geral, Histórico, Configurações, tour guiado) — deriva de `GET /api/manage/barbers`, sem toggle manual.
- Agenda: mostra espaços livres dentro do horário de funcionamento (grade desktop + lista mobile) e a view "Semana" vira lista vertical por dia em telas pequenas (estilo Calendário do iPhone) em vez da grade de 7 colunas.
- Planos de assinatura pra clientes (Configurações → Planos para clientes) ganharam botão de editar (nome/preço/benefício), além de ativar/desativar.
- Tour guiado (`webroot/assets/tour.js`, compartilhado com `barber.html` e com o `odonto-saas`): scroll até o elemento em destaque usa `behavior: "auto"` (não `"smooth"` — rolagem suave não terminava a tempo de medir a posição final em passos com alvo bem abaixo na página); botões de ação (Pular/Próximo) ficam sticky na base do tooltip, com teto de altura + scroll interno no mobile.
- `scripts/seed-solo-barbershop.ts`: cria/enriquece a barbearia de demonstração "Barbearia Solo" (1 barbeiro só), pra testar o modo acima sem mexer na "Barbearia Vintage" original — idempotente, seguro rodar de novo.
- **Telemetria de custo de IA**: toda chamada do bot ao `client.messages.create` grava tokens (`ChatUsageLog`) via `src/modules/chat/chatUsage.ts` — antes não havia nenhum jeito de saber o consumo real. `scripts/chat-usage-report.ts --days=N` soma por barbearia/período e converte pro custo real em USD.
- **SDK da Anthropic atualizado** (`@anthropic-ai/sdk` 0.68 → 0.116) e o bot passou a rodar em `output_config: { effort: "low" }` — fluxo é mecânico (seguir passos, chamar ferramentas), não precisa do thinking adaptativo em effort alto do `claude-sonnet-5`. Validado manualmente que a conversão de data relativa continua correta. **Superado em 06/09 (commit `4c3848e`) — voltou pra `effort: "high"`**, ver bug abaixo: essa suposição de "fluxo mecânico" não se sustentou pra cálculo de data.
  - **Bug real encontrado e corrigido (04/09, commits `debcbca`/`8a32f1f`)**: em teste real via WhatsApp (Barbearia Vintage), a IA converteu "amanhã" pra `2026-08-04` repetidas vezes (2 agendamentos fantasma criados, ids 2240/2241) na MESMA sessão de teste — uma conversa de 100 mensagens acumuladas ao longo de dias, com 12 menções a datas de agosto e zero a setembro. `buildDynamicContext` (`chatEngine.ts`) sempre passava a data certa no contexto (recalculada do zero a cada chamada, não é cache/sessão antiga) — a causa era o modelo se ancorando no padrão de datas do próprio histórico da conversa em vez da instrução explícita de "hoje é", mais provável rodando em `effort: "low"`. **Confirmado por teste isolado**: a mesma pergunta numa sessão nova (sem histórico) computou a data certa (`2026-09-05`) de primeira, ponta a ponta até criar o agendamento real (id 2265, validado no banco, depois limpo).
    - **Correção em duas camadas** (não depende só da IA acertar): `getAvailableSlots` (`appointments.service.ts`) agora retorna `[]` imediatamente pra qualquer data anterior a hoje — antes só filtrava horário já passado *dentro* do dia de hoje, uma data inteira no passado passava reto como se fosse futura válida (essa era a falha mecânica que deixava o agendamento errado passar). `createAppointment` rejeita de novo, independente, mesmo se chamado sem passar por `getAvailableSlots` antes. Erro vira `tool_result` com `is_error: true` — a IA recebe e se recupera na conversa. Reforço adicional no prompt (`buildDynamicContext`) instruindo a IA a ignorar datas de mensagens antigas ao calcular "hoje"/"amanhã" — mitigação, não a garantia (quem garante é a trava mecânica).
    - Teste de regressão permanente em `appointments.service.test.ts`. Sessão de teste real (`1:554797760610`) resetada.
    - **Lição**: sessão de teste reaproveitada por dias/semanas acumula viés de data que pode enganar a IA mesmo com o contexto correto — sempre que investigar comportamento estranho da IA envolvendo data/hora, testar primeiro numa sessão nova antes de suspeitar de bug de código.
  - **Segundo bug relacionado, mais profundo (06/09, commit `1762031`)**: dois dias depois do agendamento real acima (id 2288, 05/09), o mesmo cliente perguntou "tem cabelo pra amanhã?" — a IA respondeu "você já tem um agendamento pra amanhã (05/09)", mesma data errada, mas dessa vez com "hoje é domingo, 2026-09-06" **corretamente presente** no prompt. Não era mais erro de cálculo de data. **5 experimentos isolados** (reproduzindo a mesma sessão real, sem editar conteúdo) até achar a causa: `effort: "high"` não mudou nada; injetar a lista real de agendamentos futuros no contexto (vazia, correta) não mudou nada — a IA ignorou o dado fresco; proibição explícita e repetida no prompt ("NÃO diga que ele tem um agendamento") não mudou nada; apagar a palavra "amanhã"/a data do texto livre do histórico não mudou nada (a IA ainda afirmou "você já tem um agendamento", só sem data — prova que não era cópia de texto, era crença persistente); só parou de acontecer removendo **todo vestígio** do agendamento antigo do histórico (tool_use + tool_result + texto livre que cita o link .ics). Causa raiz: o `tool_result` antigo (`"confirmado":true`, data, horário) continua visível no histórico da conversa e pesa mais que qualquer instrução nova, não importa quão explícita — comportamento do modelo, não bug de prompt.
    - **Correção**: `pruneStaleAppointmentHistory` (`chatEngine.ts`) remove, só na cópia mandada pra API (o histórico completo continua salvo no banco pro painel de Conversas mostrar tudo), as trocas de `criar_agendamento`/`reagendar_agendamento` cuja data já passou. Confirmado por teste reproduzindo a sessão real duas vezes seguidas, sem edição manual: a IA parou de afirmar o agendamento fantasma e calculou "amanhã" certo (07/09) nas duas. 4 testes de regressão novos em `chatEngine.test.ts`.
    - **Lição maior**: pra esse modelo/effort, uma vez que um fato (mesmo desatualizado) entra no histórico via `tool_result`, nenhuma instrução no system prompt — por mais explícita, repetida ou reforçada com dado fresco — confiavelmente o corrige. A única correção que funcionou de verdade foi remover o dado velho de circulação. Vale lembrar disso antes de tentar "resolver no prompt" qualquer alucinação parecida no futuro — testar a hipótese de poda de histórico bem mais cedo.
  - **Reforço no mesmo dia (commit `d791916`)**: a poda só reconhecia o link `.ics` — mas o pedido de avaliação pendente (legítimo, cita a data em prosa sem link) "contaminou" o texto de resposta do próprio turno ("você já tem um agendamento pra amanhã") e essa frase nova, sem link, escapou do filtro e virou o novo ponto de ancoragem no turno seguinte. `pruneStaleAppointmentHistory` agora também casa por variantes de texto da data (DD/MM e DD/MM/AAAA), não só pelo link.
  - **Bug crítico introduzido pela própria correção, achado numa bateria de testes de múltiplos turnos no mesmo dia (commit `87975ce`, HOTFIX)**: `pruneStaleAppointmentHistory` devolvia a MESMA referência do array de entrada no caminho mais comum (nada pra podar ainda, sessão nova). `sendMessage` trata o retorno (`apiMessages`, só o que vai pra API) e `session.messages` (histórico completo, salvo no banco) como duas variáveis independentes — mas como apontavam pro mesmo array, `session.messages.push(...)` mutava os dois ao mesmo tempo, e o `apiMessages = [...apiMessages, novoItem]` duplicava a mensagem que aquele push já tinha acrescentado. A Anthropic rejeitava com `"tool_use ids must be unique"` assim que a conversa passava do primeiro turno — **toda conversa de 2+ turnos sem agendamento antigo no histórico (o caso mais comum) quebrava**. Corrigido: a função sempre devolve um array novo (spread), nunca a mesma referência recebida. Achado e corrigido no mesmo dia do deploy que introduziu o bug, antes de qualquer relato real de cliente — bateria de 9 cenários (data explícita futura/passada, "hoje", agendamento antigo x futuro no histórico, cancelar, reagendar, lista de espera, perguntas gerais) todos passando depois do fix.
    - **Lição**: sempre que uma função de poda/filtro de array tiver um caminho de "nada pra fazer, devolve como veio", checar explicitamente se está devolvendo a MESMA referência ou uma cópia — a diferença só aparece em bugs de mutação compartilhada bem depois, difíceis de linkar à causa.
  - **Causa final, resolvida de fato (06/09, commit `4c3848e`)**: mesmo com o histórico 100% limpo (sem contaminação nenhuma), o usuário pegou a IA errando "amanhã" de novo — dessa vez claramente não era mais bug de código, era o modelo variando entre chamadas idênticas (LLM não é 100% determinístico). **Medido, não estimado**: repetindo a mesma pergunta real 6 e depois 8 vezes seguidas no histórico real (já limpo), `effort: "low"` acertou só 5/6 e 6/8 — um erro residual real, não hipotético. Rodando as mesmas 14 repetições com `effort: "high"`: **14/14**. Trocado de vez (`output_config: { effort: "high" }`) — o texto acima que dizia "fluxo é mecânico, não precisa de raciocínio profundo" (03/09) não se sustentou: cálculo de data relativa e desambiguação de intenção do cliente se beneficiam de mais raciocínio, sim. Custo maior de token/latência aceito.
  - Também nessa investigação: `verificar_horarios_disponiveis` (`chatEngine.ts`) ganhou um campo `erro` explícito quando a data pedida já passou (antes só devolvia `horarios_disponiveis: []`, e a IA lia isso como "sem vaga" em vez de "data errada, recalcule") — ajuda a IA a se autocorrigir dentro do mesmo turno. E reforço de formato: instrução explícita pra nunca mostrar data em `AAAA-MM-DD` pro cliente (achado no mesmo teste, a IA mostrou "2026-09-05" numa confirmação em vez de "05/09").
  - **Lição maior de verdade**: `effort: "low"` é uma otimização de custo que carrega risco real pra qualquer decisão que dependa de aritmética ou desambiguação (não só "fluxo mecânico de chamar ferramenta") — medir taxa de erro repetindo a mesma pergunta várias vezes é o jeito certo de validar isso, uma tentativa única (mesmo que passe) não prova nada sobre um sistema não-determinístico.
  - Depois disso, com effort já em `medium`/`high` medido 14/14, ainda apareceu **mais um caso real em produção (06/09, 22h13 em Brasília)**: cliente pediu "barba pra amanhã" e o bot respondeu "amanhã na verdade é 08/09" — dois dias à frente do real (07/09), não um. Dessa vez a causa **não era o modelo**, era mecânica e 100% reproduzível: `buildDynamicContext`/`verificar_horarios_disponiveis`/`pruneStaleAppointmentHistory` (`chatEngine.ts`) e `getAvailableSlots`/`createAppointment` (`appointments.service.ts`) calculavam "hoje" com `new Date().toISOString().slice(0,10)` — **`toISOString()` é sempre UTC**, independente de `process.env.TZ`. `src/lib/timezone.ts` força `TZ=America/Sao_Paulo` no boot, mas isso só afeta os getters locais do `Date` (`getFullYear`/`getMonth`/`getDate`), nunca `toISOString()`. Resultado: **toda noite, das 21h às 23h59 em Brasília (UTC-3), o servidor já considerava "hoje" o dia seguinte** — confirmado com `new Date('2026-09-06T22:13:00-03:00').toISOString()` → `2026-09-07T01:13:00.000Z`. Corrigido (commit a seguir) trocando os 5 pontos por `localDateStr(new Date())` (`src/lib/time.ts`, já usado em outros lugares do código exatamente por respeitar `TZ`). Validado 3x contra uma sessão de teste reproduzindo o horário exato do bug (relógio congelado via `Date` global sobrescrita, não `vi.useFakeTimers` — script fora do vitest) — as 3 vezes o bot respondeu "amanhã (07/09)" corretamente. 2 testes de regressão novos em `appointments.service.test.ts` (`vi.setSystemTime` a `22:13:00-03:00`, confirmando que `createAppointment`/`getAvailableSlots` não tratam o próprio dia de hoje como passado).
  - **Lição**: um bug "de IA" que se repete de formas ligeiramente diferentes (primeiro "ontem", depois "dois dias à frente") vale a pena checar se ainda é *dado de entrada* errado antes de assumir de novo que é o modelo variando — mesmo depois de já ter sido genuinamente o modelo da vez anterior. `toISOString()` em vez de getters locais é o tipo de bug que só aparece numa janela de poucas horas por dia (21h-23h59 em Brasília) e nunca durante testes manuais feitos de dia, o que explica por que passou despercebido em toda a bateria de testes anteriores.
- 3 features portadas do `odonto-saas`: **notas de atendimento** (campo opcional em `Appointment`, visível no histórico do cliente), **tom de voz da IA** (dono cola exemplos de mensagens reais em Configurações, viram few-shot no prompt do bot) e **reconquista em massa** (botão em Histórico que dispara a mensagem de reconquista pra todos os clientes atrasados de uma vez).
- **WhatsApp Embedded Signup (self-service) — funcionou ponta a ponta pela primeira vez em 15/08.** Depois de meses de debug (SDK carregando tarde, CSP, `FB.login()` com callback async, falso-positivo de cancelamento, App Domains/Allowed Domains faltando no painel da Meta — tudo corrigido em commits entre `2235908` e `bc6c0a2`), o `postMessage` `WA_EMBEDDED_SIGNUP`/`FINISH` finalmente chegou: a Barbearia Vintage conectou o próprio número (`phone_number_id`/`waba_id`/token salvos, status `pending_templates`, que já é sucesso — só falta a Meta aprovar os modelos de mensagem em paralelo, via webhook `message_template_status_update`). **Ainda não repetido com uma segunda conta pra confirmar que não foi coincidência** — se testar de novo, o log de debug (`console.log("[WHATSAPP CONNECT DEBUG]"...)` em `admin.html`, commit `bc6c0a2`) ainda está no ar, remover depois de mais confirmações. A permissão `business_management` (genérica, diferente de `whatsapp_business_management` que já estava aprovada) segue rejeitada pela Meta (motivo: screencast incompleto) — o reenvio ainda não foi feito, mas o teste de 15/08 sugere que talvez nem seja bloqueante na prática.
  - **Bug do template OTP: corrigido (15/08, commit `2ab3cc3`)**. Sintoma era erro real da Meta (`132018 — Button at index 0 must be of type Url`) ao mandar código pra assinatura de plano. Causa: `sendWhatsappAuthTemplate` (`src/lib/whatsapp.ts`) mandava `sub_type: "copy_code"`/`coupon_code`, mas a Meta valida/expõe templates de autenticação com botão `COPY_CODE` como `sub_type: "url"` — corrigido. Era um bug latente em **todo** fluxo de OTP, não só da Vintage. Confirmação ponta a ponta com número real ainda pendente (o de teste usado pra validar não tem verificação de negócio completa, então a Meta aceita o envio mas não entrega — problema da conta de teste, não do código; ver `[[project_whatsapp_embedded_signup_debug]]`).
  - O número conectado no teste de 15/08 era um **número de teste da própria Meta** (`+1 555-310-6678`), não o real da Vintage — o dono só queria confirmar que o botão "Conectar meu WhatsApp" funcionava (funcionou). Reconectar com o número real (`+55 47 8865-6379`) fica pra quando for conveniente.
- **Motor único concluído (15/08)**: schema e código renomeados `Barbershop→Business`/`Barber→Professional`, sistema de vocabulário por vertical (`src/config/verticals/`, `VERTICAL` env var, default `"barbearia"` aqui) trazido do `odonto-saas`, migration de rename aplicada em produção. `chatEngine.ts` (prompt + descrições de tools) vocabulário-driven — nomes das tools/params mantidos como estão de propósito. Depois de aplicar a migration, uma varredura pega vários lugares que ainda mandavam os nomes antigos (`barbershopId`/`barberId` camelCase) em corpo/query de requisição pro backend, que só aceita `businessId`/`professionalId` agora — `chat.html`, `minha-conta.html`, `excluir-dados.html`, `barber.html`, `superadmin.html` e partes de `admin.html` corrigidos. **Lição pra próximas migrações de nome assim**: `tsc` não pega esse tipo de bug porque cruza a borda JS-solto↔JSON; e scripts em `scripts/*.ts` não estão no `tsconfig.json` (só rodam via `tsx` direto), então nunca são checados — 3 deles (`seed-solo-barbershop.ts`, `seedUpcoming.ts`, `chat-usage-report.ts`) ainda usavam `prisma.barbershop`/`prisma.barber` até serem corrigidos manualmente.
- **Marca renomeada pra "barber.bot"** (15/08, era "Barbearia Bot"/"Painel Premium") em toda parte visível: landing, login, admin, `manifest.json`, notificação push, simulador de chat.
- **Landing page reconstruída** (15/08) seguindo o mesmo redesign "Claude Design"/Cal.com do `odonto-saas`: header em pill flutuante fixo (não mais `sticky` — tinha bug de "vibração" no scroll mobile, corrigido nos dois produtos), mockup de WhatsApp animado no hero. No mobile, o mock toca a animação e depois vira o texto do hero por cima (mesma célula do grid, crossfade), sem precisar rolar — ver `.hero-grid.hero-swapped` em `webroot/index.html`.
- **Planos de assinatura pra clientes — link direto por plano** (15/08, substituiu a ideia antiga de dropdown de barbearia em `minha-conta.html`): `webroot/assinar-plano.html?plan=<id>` mostra só aquele plano (nome, preço, benefício, nome da barbearia) e o fluxo nome+telefone+OTP+checkout, sem o cliente escolher a barbearia num dropdown que listava todas. `minha-conta.html` voltou a ser só "Meus agendamentos". Botão "Copiar link" em cada plano ativo (Configurações → Planos para clientes). Ver `[[project_client_subscription_plans]]`.
- **Testes HTTP fim a fim** (15/08, `supertest` adicionado): webhook do WhatsApp (`whatsapp.routes.test.ts` — handshake, assinatura HMAC, roteamento pra IA, dedupe de wamid, fallback de mensagem não-texto) e painel de super-admin (`superadmin.routes.test.ts` — guard `requireSuperAdmin`, login/logout, rotas de leitura). `vitest.config.ts` passou a carregar `.env` via `process.loadEnvFile()` — sem isso, rodar um arquivo de teste isolado falhava com `DATABASE_URL: Required` mesmo a suíte inteira funcionando junto.
- **Identidade nas rotas públicas de autoatendimento** (15/08): ver histórico, cancelar, reagendar, baixar `.ics` de agendamento e pedir exclusão LGPD agora exigem o telefone ter passado pelo código OTP do WhatsApp nos últimos 15min (`assertPhoneVerifiedRecently`) — antes bastava saber o telefone de alguém pra fazer isso em nome dela. Criar agendamento novo e ver horários livres continuam sem OTP (não revelam/alteram dado de terceiro). Verificação de telefone extraída de `clientPlans` pra `src/modules/clients/phoneVerification.{repository,service}.ts` (era genérica, só estava no lugar errado); novas rotas genéricas `POST /api/public/verify/start` e `/verify/confirm` em `clients.routes.ts`. `minha-conta.html`/`excluir-dados.html` ganharam o passo de código. O bot do WhatsApp não é afetado (chama `appointments.service.ts` direto, já confia no canal via `verifyPhoneViaTrustedChannel`).
- **Bloqueio real do painel + bot por assinatura cancelada** (16/08, portado no mesmo dia pro `odonto-saas`): antes, trial vencido/assinatura cancelada só mudava um badge na tela de Cobrança — nada impedia o dono de continuar usando o painel de graça, nem o bot de continuar respondendo os clientes. Agora `isBillingBlocked` (`billing.service.ts`, só `status === "canceled"` com Stripe configurado — nunca trialing/active/past_due, nunca barbearia sem linha de `Subscription` como as de demonstração) alimenta dois bloqueios: `requireBillingOk` (`src/middleware/billing.ts`), gate global em `app.ts` antes de todas as rotas autenticadas (exceto billing/auth/público/webhooks/superadmin, senão não daria pra se destravar), devolve 402 `billing_blocked`; e um check equivalente dentro de `chatEngine.sendMessage` que responde com aviso fixo em vez de chamar a IA (salva a mensagem do cliente e marca `needsAttention`). Frontend: `webroot/billing-required.html` (seletor de plano pro dono, aviso pro profissional) + `assets/billingGate.js` (intercepta 402 e faz checagem proativa no boot), incluído em `admin.html`/`barber.html`. Testado ponta a ponta em dev local (login bloqueado → paywall → botão "Assinar" abre checkout real do Stripe).

### Sessão de 20/08: dados de demo da Vintage, ajuste do card de Atendimentos e Google Agenda

- **Barbearia Vintage recebeu dados de demonstração completos**: `scripts/seed-vintage-history.ts` (histórico dos últimos 90 dias majoritariamente concluído + avaliações 4-5 estrelas, agenda lotada dos próximos 30 dias) e `scripts/redistribute-vintage-barbers.ts` (reativou os 4 barbeiros que estavam `active: false` — só "Wesley" tinha ficado ativo — e redistribuiu a agenda futura entre todos, sem conflito de horário). Os dois seguem o padrão idempotente dos seeds existentes (`seedUpcoming.ts`, `seed-solo-barbershop.ts`): rodar de novo só completa o que falta, não duplica.
- **Card "Conversas do WhatsApp" da aba Atendimentos**: mostrava só 5 conversas por um `.slice(0, 5)` fixo no código (não por falta de dado — a Vintage já tinha 25 conversas no banco). Medido ao vivo no dev local: o card tem 638px fixos (`.overview-grid-two .panel`), 8 itens estourava ~45px, **7 itens preenche exatamente sem sobra nem overflow**. Ajustado pra `7` em `admin.html` (commit `fe7d047`) — mesmo ajuste replicado no `odonto-saas` (commit `c26076c`, precisou rebase por causa de outra sessão paralela mexendo no mesmo arquivo).
- **Google Agenda: env vars configuradas, mas achado um bug real no caminho**: `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` foram configuradas na Vercel, porém o `redirect_uri` que o backend mandava pro Google apontava pro **domínio antigo** (`barbearia-saas-jet.vercel.app`) — `redirectUri()` em `googleCalendar.routes.ts` usa `env.PUBLIC_BASE_URL` (que estava com o domínio velho, provavelmente esquecido desde a migração de domínio) antes de cair no host da requisição. Isso quebra o OAuth de verdade (o Google rejeita com `redirect_uri_mismatch` se a URI cadastrada no Console não bater exatamente), diferente de outros usos de `PUBLIC_BASE_URL` (reset de senha, confirmação de agendamento, checkout) que "sobrevivem" porque o domínio antigo só redireciona (307) e o navegador segue. **Corrigido só na env var da Vercel** (`PUBLIC_BASE_URL` → `https://agenda-barb.vercel.app`), sem mudança de código — confirmado via `GET /api/manage/google-calendar/connect` retornando a URI certa. Vale conferir se o `odonto-saas` tem a mesma env var desatualizada.
- **Testando a conexão de verdade, apareceu um segundo obstáculo (esperado, não é bug)**: erro 403 `access_denied` do Google — "app não concluiu o processo de verificação... só pode ser acessado por testadores aprovados". O app OAuth no Google Cloud Console está em modo **"Testing"**, que só libera contas cadastradas em **APIs & Services → OAuth consent screen → Test users**. Resolve adicionando lá os e-mails que forem testar/usar (inclui contas de barbearias clientes, uma por uma, até publicar o app de verdade). Publicar o app pra produção pode disparar pedido de verificação do Google pelo escopo `calendar.events` ser sensível — processo parecido com o que já rolou pro `business_management` do WhatsApp.
- **Bloqueador real achado em 10/09, pra quando for publicar/verificar o app de verdade**: o processo de verificação do Google exige comprovar posse de cada "authorized domain" via **Google Search Console** — e isso **não funciona** com `agenda-barb.vercel.app`, porque é um subdomínio de `vercel.app` (domínio de hospedagem compartilhada, dono é a Vercel, não a gente) — o Search Console não verifica posse de um "top private domain" que não é seu. Confirmado contra a doc oficial do Google, não é suposição. **Não dá pra automatizar adicionando test users via API também** — a API antiga do Identity-Aware Proxy que fazia isso foi desativada em março/2026, hoje só dá pra adicionar test user manualmente pelo Console, um por um. Decisão do usuário (10/09): **adiar** — precisa de um domínio próprio (não `*.vercel.app`) apontando pro Vercel antes de sequer tentar submeter pra verificação. Revisitar quando o domínio próprio existir.

### Sessão de 23/08: Google Agenda conectado mas evento não aparecia — bug de fire-and-forget em serverless

- **Sintoma**: dono conectou o Google Agenda de verdade (Diego, na Vintage), mas nenhum evento aparecia lá depois de criar agendamento.
- **Causa raiz**: `mirrorAppointmentToGoogle`/`removeAppointmentFromGoogle` eram chamados como fire-and-forget (`.catch(() => {})`, sem `await`) dentro de `createAppointment`/`rescheduleAppointment`/`cancelAppointment` (`appointments.service.ts`) — a rota HTTP respondia antes da chamada de rede pro Google terminar. Isso é inofensivo num servidor tradicional (processo fica de pé), mas **quebra em função serverless da Vercel**: assim que a resposta HTTP é enviada, a execução pode ser congelada a qualquer momento, matando a Promise solta antes dela completar. Resultado: `googleEventId` nunca gravava no banco, silenciosamente (o próprio código já engolia erro por design, então nem log de falha aparecia).
- **Corrigido (commit `3cb6c0b`)**: trocado fire-and-forget por `await` nos 3 pontos. Como as duas funções já nunca lançam exceção (só logam e retornam `null`/`void`), isso não muda a garantia de "nunca bloqueia o agendamento em si" — só garante que a chamada de rede realmente termina antes da função serverless poder ser congelada. Latência extra só afeta barbearias com Google conectado (a maioria retorna cedo, sem custo).
- **Confirmado ponta a ponta em produção** (depois de promover o deploy manualmente na Vercel — não subiu sozinho): agendamento de teste criado pra Diego (Vintage) gravou `googleEventId` real e a chamada ficou visivelmente mais lenta (~1s vs ~300ms antes, tempo da chamada de rede pro Google). Mesmo teste replicado no `odonto-saas` (Rafael, Odonto Dent) — funcionou de primeira, sem precisar promover manual lá.
- Mesmo fix portado pro `odonto-saas` no mesmo commit-equivalente (`54abe7d`).

### Sessão de 29/08: análise de diferenciais do Trinks + item 1 (fechamento de comissão)

- **Análise comparativa com o Trinks** (concorrente, painel mapeado ao vivo via login do usuário): levantamento de features que existem lá e não aqui, revisado contra o código de verdade (não só suposição) — comissão e estoque já existiam parcialmente, corrigido na segunda passada. Plano final ordenado por impacto, com gorjeta/pacote pré-pago/site público de agendamento fora de escopo (decisão do usuário) e maquininha/fila de espera redefinidos (reconciliação manual em vez de TEF; lista de espera via bot de WhatsApp em vez de tela de balcão). Ver `[[project_saas_rewrite]]` — vale revisitar esse plano quando for portar os próximos itens pro `odonto-saas`.
- **Item 1 implementado: fechamento e pagamento de comissão por profissional** (commit `d629fc8`). Novo model `ProfessionalPayout` — o dono fecha a comissão do período (mesmo cálculo de `dashboardStats.service.ts`, serviço + produto) e depois marca como pago, com ajuste manual opcional (correção, desconto combinado) e histórico auditável; antes a comissão só existia como leitura no dashboard, sem virar ação. `src/modules/payouts/` (repository + rotas `/api/manage/payouts/*` e `/api/payouts/my-history`), card "Comissão dos barbeiros" na aba Métricas (`admin.html`), escondido em modo barbeiro-único como o resto da UI de comissão. Migration `20260829000000_add_professional_payouts` aplicada em produção via `prisma migrate deploy`.
- **Achado durante o teste manual**: o fluxo original usava `prompt()`/`confirm()` nativo do navegador pro ajuste no pagamento — sem precedente real no app (só um `confirm()` solto em `superadmin.html`) e trava a renderização da página. Trocado por formulário inline no mesmo padrão de `edit-product-form` (toggle de `.inline-form`).
- **Bug de fuso pego no mesmo teste**: `toApiPayout` (`src/lib/apiMappers.ts`) usava `localDateStr` (hora local) pra formatar `periodStart`/`periodEnd`, que são colunas `@db.Date` (meia-noite UTC) — deslocava a data um dia pra trás em fuso negativo (Brasil), ex: fechar "01/08 a 29/08" aparecia como "31/07 a 28/08". Corrigido com `dateToStr` baseado em ISO, mesmo padrão que `appointments.types.ts` já usa pra esse tipo de coluna — vale conferir se outro `toApi*` novo comete o mesmo erro no futuro.
- Ainda não portado pro `odonto-saas` — fazer depois de mais uso real aqui.

**Itens 2-7 implementados na sequência, mesmo dia** (commit `0f9e791`, migration `20260829010000_financeiro_estoque_cupons_lista_espera`):

- **Contas a pagar**: model `Expense`, CRUD em `src/modules/expenses/`, nova aba **Financeiro** na sidebar do `admin.html` (antes não existia página própria de financeiro — os KPIs viviam só em Métricas).
- **Forma de pagamento manual**: `Appointment.paymentMethod` (dinheiro/pix/cartão/outro), editável no formulário de agendamento existente. É reconciliação manual, não integração TEF — decisão registrada aqui: TEF de verdade amarra a UMA marca de adquirente (não existe "integrar com qualquer maquininha que o cliente já tenha"), exige virar parceiro homologado, e fica de fora até escolher um parceiro (candidato natural: Stone, já usado no WhatsApp Embedded Signup).
- **Cupons**: model `Coupon`, `src/modules/coupons/`, aba Financeiro. Aplicado só manualmente ao editar agendamento no painel (`updateAppointmentDetails`) — o bot de WhatsApp nunca oferece cupom sozinho, de propósito (evita a IA "inventar" desconto). Reaproveita `Appointment.priceChargedCents`, mesmo campo que `ClientPlan` já usava.
- **Fornecedor + movimentação de estoque**: models `Supplier`/`StockMovement`. Toda mudança de `Product.stockQuantity` (venda, criação, edição manual) agora grava uma linha auditável em `stock_movements` — antes só existia o saldo atual, sem histórico. UI nova na aba Estoque.
- **Lista de espera via bot de WhatsApp**: model `WaitlistEntry` + tool `entrar_lista_espera` em `chatEngine.ts` — quando `verificar_horarios_disponiveis`/`buscar_proximo_horario_disponivel` não acham nada no período pedido, o bot oferece entrar na lista; `cancelAppointment` (`appointments.service.ts`) notifica automaticamente quem está esperando um horário que combina (mesmo profissional/serviço, dentro do período aceito). Notificação é texto livre via WhatsApp (não Message Template), então só chega garantido dentro da janela de 24h — cobrir fora disso exigiria pedir um template dedicado à Meta, fora do que dava pra fazer só em código. Viewer de leitura em Clientes.
- **Bug pego no teste manual**: a lista de Movimentação de estoque não recarregava sozinha após criar/editar produto (backend gravava certo, frontend não dava refresh) — corrigido.
- Testes de integração novos: `couponApply.service.test.ts` (desconto percentual/fixo, cupom inativo, não empilha dois cupons) e `waitlist.service.test.ts` (notifica só quem bate profissional+período no cancelamento). Suíte: 28 arquivos, 167 testes.
- **Fora de escopo nesta passada** (pedido do usuário ou dependência externa): gorjeta, pacote de serviço pré-pago, site público de agendamento, perfis de acesso granulares, vale-presente, programa de fidelidade, TEF real com adquirente parceiro.
- Nada disso foi portado pro `odonto-saas` ainda.

**Nota Fiscal pesquisada, mas não implementada (29/08)**: existe alternativa gratuita do governo (NFS-e Nacional, Receita Federal, API REST/JSON, sem mensalidade tipo Focus NFe/PlugNotas) — mas emissão automática via API exige certificado digital ICP-Brasil (A1/A3) **por CNPJ** (R$150-350/ano cada), sem procuração eletrônica ainda (só pleito da FENACON pendente), então não dá pra centralizar com uma credencial só da plataforma — cada barbearia teria que subir o próprio certificado. Usuário decidiu pular por enquanto. Se retomar: a ideia validada foi opcional (sem certificado = só ajuda a preencher os dados pro dono emitir manual no portal gratuito; com certificado = automação real via API nacional).

**Item 2b/2c implementado (29/08, commit `1e996a1`): contas financeiras + caixa.** Models `FinancialAccount` (caixa/banco) e `CashSession` — `src/modules/financialAccounts/`, `src/modules/cashSessions/`. "Esperado" ao fechar o caixa é calculado on demand (soma de `Appointment`/`ProductSale` com `paymentMethod` "dinheiro" no dia da abertura), nunca gravado até o fechamento de verdade — editar um agendamento antigo não altera retroativamente um turno já fechado. `ProductSale` ganhou `paymentMethod` nessa mesma migration (`20260829020000_financeiro_contas_caixa`), por paridade com `Appointment` — venda de produto numa visita herda a forma de pagamento escolhida pro agendamento inteiro. UI nova em Financeiro (abrir/fechar turno, esperado ao vivo, histórico com diferença). Fora de escopo: sangria/despesa dentro da sessão, transferência entre contas, saldo bancário automático.

### WhatsApp Coexistence implementado (29/08, commit `5f1449c`)

**Tech Provider aprovado** — confirmado pelo usuário direto no App Dashboard da Meta (Business Verification + App Review & Access Verification + Integrity, os 3 verdes; "You can use this app in production now"). O gate antigo no código (`WHATSAPP_APP_ID`/`WHATSAPP_CONFIG_ID` vazios = não aprovado) estava desatualizado — essas duas variáveis já estavam preenchidas há tempos pelo Embedded Signup normal (que não precisa de Tech Provider), então não serviam mais de sinal.

- Dono agora escolhe entre **Coexistence** (mantém o WhatsApp Business App normal no celular, IA responde em paralelo no mesmo número — padrão pré-selecionado, recomendado) ou **conectar um número novo/migrar** (comportamento antigo, único que existia antes). Seletor em Configurações → WhatsApp, passos explicados mudam conforme a escolha.
- `featureType: "whatsapp_business_app_onboarding"` no `FB.login()` quando Coexistence é escolhido. O evento de finalização é outro (`FINISH_WHATSAPP_BUSINESS_APP_ONBOARDING`, não `FINISH`) — confirmado contra a doc oficial da Meta antes de implementar, não chutado.
- `Business.whatsappCoexistence` (migration `20260829030000_whatsapp_coexistence`). No callback (`whatsappConnect.routes.ts`), Coexistence pula geração de PIN e a chamada a `/register` — o número já está registrado pelo próprio WhatsApp Business App; registrar de novo é desnecessário e arriscaria derrubar a sessão do app no celular do dono.
- Teste HTTP novo (`whatsappConnect.routes.test.ts`) — o módulo nunca tinha teste antes.

**Validado ponta a ponta em produção em 04/09** (Barbearia Vintage, número real `+55 47 8865-6379`) — depois de dois bugs reais encontrados e corrigidos no processo:

- **Faltava uma Configuration ID dedicada pro Coexistence** (commit `073d9e3`). O Configuration Builder da Meta não tem um toggle de Coexistence dentro da mesma Configuration usada pro fluxo padrão — é uma variação de login separada, precisa de uma Configuration própria criada do zero no App Dashboard. Sem isso, mesmo com `featureType` certo no `FB.login()`, a Meta tratava o número como registro novo e mostrava "already registered... migrate or disconnect" em vez do QR Code. `WHATSAPP_CONFIG_ID_COEXISTENCE` nova (opcional, cai pro `WHATSAPP_CONFIG_ID` padrão se não configurada) — mesmo padrão já usado no `sistema-dgs` (que também nunca tinha chegado a criar essa Configuration separada). Configuration nova criada: ID `1516462183584824` ("Agenda SaaS"), env var configurada na Vercel.
- **Status "error" sendo tratado como desconectado no painel** (commit `42907df`). `whatsappConnectionStatus` vira `"error"` quando a Meta rejeita um Message Template (`handleTemplateStatusUpdate`, `whatsapp.routes.ts`) — mas isso **não** significa que o número está desconectado, só que lembrete/reagendamento/reconquista/OTP automáticos ficam pausados (o bot responde normal dentro da janela de 24h). O `admin.html` tratava `"error"` como `showConnectFlow`, escondendo que a conexão real funcionava e mostrando de novo a tela de "Como você quer conectar?" — muito confuso pro dono, que já via o WhatsApp Business App conectado no próprio celular. Corrigido: `showConnectFlow` só pra `"not_connected"` de verdade; texto do aviso reescrito pra descrever o problema real. Mesmo bug existia em `has_own_whatsapp` (`billing.routes.ts`), corrigido junto.
- **QR Code/código de acesso expiram rápido** (~1min) — se demorar entre o código aparecer na tela e escanear/digitar no celular, vira "Invalid verification code"/"Couldn't scan code". Não é bug, é só agir rápido na hora de confirmar.
- **Pendente**: os 3 templates da Vintage foram **rejeitados pela Meta** nessa WABA nova (motivo genérico "doesn't comply with one or more policies", sem detalhe — precisa abrir cada um no WhatsApp Manager e pedir revisão via "Business Support Home" pra saber o motivo exato). Até resolver isso, lembrete/reagendamento/reconquista/OTP automáticos continuam pausados pra essa conexão especificamente, mesmo com o número já funcionando pro atendimento normal.

**Hotfix de regressão real em produção, achado testando isso**: desde o commit `1e996a1` ("Contas financeiras e caixa"), uma `<div class="card panel">` duplicada na seção "Contas a pagar" (`admin.html`) deixava `page-financeiro` sem fechar — Clientes, Estoque, Configurações e Auditoria ficavam aninhados dentro dela, herdando `display:none` e aparecendo em branco ao clicar nessas abas. Corrigido no mesmo commit `5f1449c`; as 12 páginas do painel foram verificadas uma a uma (altura real, `offsetParent` não-nulo) depois do fix. **Lição**: ao inserir HTML grande via Edit com `old_string` terminando num `<div>` de abertura, conferir que não sobrou abertura duplicada — o `tsc`/`vitest` não pegam esse tipo de bug (é só estrutura de HTML), só um teste visual/de altura no browser pega.

### Projeto Ponte — extensão de WhatsApp em cima de sistemas de agenda de terceiros (29/08, iniciado)

Pivô comercial: um cliente atual quer só a camada de IA no WhatsApp, plugada em cima do **AppBarber** (o
sistema que ele já usa e não quer trocar — é vitrine dele pra clientes novos), em vez do painel completo.
Plano detalhado em [`docs/projeto-ponte.md`](docs/projeto-ponte.md) — resumo do que já foi decidido:

- **AppBarber é fechado**: sem API pública, sem webhook, sem programa de parceiros documentado (contato:
  `atendimento@appbarber.com.br`). Concorrente direto (Trinks) tem API pública + webhooks e cita
  "agendamento via chatbot" como caso de uso oficial — valida o modelo de negócio, só não vale pro AppBarber.
- **Achado crítico**: a tela de login do AppBarber (`sistema.appbarber.com.br/login.php`) tem reCAPTCHA
  invisível da Google. Decisão: não tentar contornar (nem via serviço de resolução de captcha — seria burlar
  proteção anti-abuso de terceiro). Saída escolhida: **sessão persistente** — o dono loga manualmente uma vez
  (passa pelo captcha ele mesmo), o worker reusa a sessão salva pra automatizar o resto sem repetir login.
- **Código mora em `worker/`**, pasta própria na raiz do repo com `package.json`/`tsconfig.json`
  independentes do app principal — sem `workspaces` declarado, então o `npm install`/build da Vercel nem
  toca nela (Playwright/Chromium não cabe numa function serverless). Roda fora da Vercel quando virar
  produção (VPS/Railway/Fly.io). **Tem checagem de tipo própria** (`npm run typecheck` dentro de `worker/`) —
  o `tsc` da raiz não cobre essa pasta, mesma armadilha que já mordeu os `scripts/*.ts` uma vez.
- **Fase 1 (spike de viabilidade) esqueletada, ainda não validada contra conta real**: `worker/src/spike/capture-session.ts`
  (login manual numa janela de navegador de verdade, salva a sessão) + `worker/src/spike/create-appointment.ts`
  (headless, reusa a sessão salva, confirma que chega autenticado sem cair no login de novo — a parte de criar
  o agendamento em si está com `TODO` marcado, esperando alguém rodar contra uma conta de teste real pra
  descobrir os seletores da tela autenticada). Ver `worker/README.md` pro passo a passo.
- Próximo passo depende de alguém (não da IA) rodar os dois scripts contra uma conta de **teste** do AppBarber
  (nunca conta real de cliente) — só então dá pra confirmar se a Fase 1 realmente funciona e seguir pra Fase 2
  (fila de jobs, cofre de credenciais/sessão criptografado no banco, alerta de quebra de seletor).

### Sessão de 08/09: plano pros itens 3-10 do checklist pré-venda

Levantamento do estado real de cada pendência (não bloqueiam venda, mas valem resolver) antes de propor passos — várias tinham suposição desatualizada:

- **Ganhos rápidos aplicados no mesmo dia**: `npm audit fix` limpo (cadeia `qs`/`body-parser`, moderate) + `overrides.qs: "^6.16.0"` no `package.json` pra forçar a versão corrigida mesmo na cópia que o `express` declara direto (`~6.15.1`) — sem bump de major, 11→8 vulnerabilidades. As 8 restantes são só `prisma`/`@prisma/config` (high, via `deepmerge-ts`) e a cadeia do `vitest`/`vite`/`esbuild` (critical/moderate, dev-only) — ambas só resolvem com major, ver item 7 abaixo. `console.error` "soltos" sem captura equivalente no Sentry (`src/lib/whatsapp.ts`, `src/modules/whatsappConnect/whatsappConnect.service.ts`, `errorLog` da sessão e o cron de iCal em `src/app.ts`) agora chamam `captureError` — mitigação pra retenção curta de log do plano Hobby da Vercel (item 6) sem precisar de upgrade.
- **Item 7 (`npm audit`/Express 5/Prisma major) — RESOLVIDO em 08/09 (Express e vitest; Prisma decidido não migrar agora)**: código não usava nenhum padrão que quebra na v5 do Express (sem `app.del`, sem rota wildcard/regex, sem `app.param`, sem `req.query` aninhado) — confirmado, e a migração (`da39d82`) trouxe de brinde a correção do problema que motivava um wrapper de async handler dedicado (promise rejeitada em handler assíncrono agora vira `next(err)` sozinha). `vitest` 2→4 feito primeiro (`bc2ef28`, dev-only, fecha vuln crítica de esbuild/vite) — expôs duas correções pontuais (`import.meta.dirname` no lugar de `__dirname`, mock de construtor com `function` em vez de arrow) e um bug de flakiness real nos testes por rodarem mais paralelos por padrão (suíte bate no banco único de produção — `fileParallelism: false` resolveu). Prisma major decidido **não migrar agora**: pesquisa contra a doc oficial mostrou que exige driver adapter (`@prisma/adapter-pg`) obrigatório, generator novo (import muda de `@prisma/client` pra caminho gerado, ~10+ arquivos afetados) e `prisma.config.ts` novo — a vulnerabilidade que motivava isso (`deepmerge-ts`, via `@prisma/config`) só existe na cadeia do `prisma` CLI (devDependency), nunca em `@prisma/client` (produção); não vale o risco sem staging (item 5). Revisitar quando houver banco de staging de verdade.
- **Item 9 (frontend) — RESOLVIDO em 10/09**: os dois componentes que faltavam (`.plan-card` em Cobrança, `.tipo-card` em Serviços) foram portados do `odonto-saas` (`webroot/admin.html:514-550` lá), CSS quase 1:1 (só `professional_limit`→`barber_limit`, "dentistas"→"barbeiros"). Os dois riscos que tinham motivado deixar de fora no port de 15/08 (commit `66ab687`) foram encarados: **Cobrança** — `loadBillingStatus` reescrita pro padrão "todos os planos lado a lado sempre, atual destacado" (antes: com assinatura ativa, o card sumia e sobrava só um botão de texto "Migrar para X"); a rota `/api/billing/status` não expunha preço em centavos (só o label com o preço embutido no texto) — `PLAN_PRICE_CENTS` (já existia, usado só na estimativa de MRR do superadmin) agora também vai no payload (`billing.routes.ts`). **Serviços** — a edição inline (`edit-service-form-${id}`) migrou pra dentro do `.tipo-card` sem reescrever a lógica dos handlers (`data-edit-service`/`data-save-service` etc. idênticos), só trocando o wrapper de `.item-row` (lista) pra `.tipo-card` (grid) — cuidado extra na hora de fechar a `<div>` do card (mesma classe de bug do "`<div>` não fechada" já visto no financeiro em 29/08). Validado nos dois temas e nas 12 páginas do painel (nenhum vazamento) via Browser pane em dev local. Ainda não portado pro `odonto-saas` (lá o componente já existe desde antes, nada a fazer).
- **Item 5 (banco único) e item 6 (Vercel Hobby)**: sem ação de código pendente além da mitigação de Sentry acima — são decisões de custo/momento do usuário (staging pago, upgrade de plano), não bugs a corrigir.
- **Prisma major (parte do item 7) — decisão (08/09): não migrar agora.** A pesquisa contra a doc oficial mostrou que a suposição inicial ("bump de versão, reconferir tipos") estava errada — Prisma 7 remove o engine Rust, exige driver adapter (`@prisma/adapter-pg`) obrigatório, troca o generator (`prisma-client-js`→`prisma-client`, `output` obrigatório, import muda de `@prisma/client` pra um caminho gerado — afeta ~10+ arquivos) e um `prisma.config.ts` novo. Além disso, a vulnerabilidade que motivava isso (`deepmerge-ts`, via `@prisma/config`) só existe na cadeia do `prisma` CLI (devDependency — `migrate`/`generate`/`studio`), **nunca no `@prisma/client`** que roda em produção (confirmado com `npm ls @prisma/config`) — não é exposição real, só deixa o `npm audit` com 3 "high" cosméticos. Não vale o risco de mexer na camada de conexão (perto do ajuste sensível de pool `DATABASE_URL`/`DIRECT_URL`) sem staging (item 5) só por isso. Revisitar quando houver um banco de staging de verdade.
- **Item 8 (custo real por cliente)**: `scripts/chat-usage-report.ts` conferido contra o pricing oficial do Sonnet 5 — já está correto, só falta volume real de uso ao longo do tempo.

### Sessão de 09/09: RLS habilitado em produção + causa real da rejeição dos templates da Vintage

- **Row Level Security habilitado nas 35 tabelas do schema `public`** (migration `20260908010000_enable_row_level_security`, commit `c45d062`). Achado a partir de um alerta automático real do Supabase Security Advisor ("Table publicly accessible", crítico) — confirmado que era sobre o banco de produção de verdade (referência do projeto bate com `DATABASE_URL`/`DIRECT_URL`). RLS estava desligado desde sempre: qualquer um com a URL do projeto e a chave `anon` (pública por design no Supabase) conseguia ler/editar/apagar tudo via API REST automática (PostgREST), completamente à parte do Prisma. Confirmado antes de aplicar: a conexão da app (role `postgres`) tem `BYPASSRLS=true` — Prisma ignora RLS sempre, então isso não muda nada no funcionamento; só fecha a exposição pras roles `anon`/`authenticated` (que não têm bypass), que passam a não enxergar nenhuma linha (nega tudo, nenhuma policy criada). A chave `anon` nunca apareceu em nenhum lugar do código/histórico do repo (sem `@supabase/supabase-js` instalado) — não foi distribuída por aqui, mas continuava acessível a quem tivesse (ou já teve) acesso ao painel do Supabase. Deixado de fora: `_prisma_migrations` (tabela interna do Prisma CLI).
- **Causa real da rejeição dos 3 templates da Vintage, achada consultando a API do Meta direto** (não só o motivo genérico "doesn't comply with policies" documentado antes) — `GET /{waba-id}/message_templates?fields=...,rejected_reason` devolveu `rejected_reason: "INVALID_FORMAT"` nos 3. Causa raiz confirmada no código: `createTemplates` (`whatsappConnect.service.ts`) montava o componente `BODY` só com `text`, sem `example.body_text` — a Meta **exige** um valor de exemplo pra cada `{{n}}` usado no corpo quando o template é criado via API (não é só recomendação, é rejeição automática sem isso). Achado secundário: `come_back_message` (mensagem de reconquista) era submetido como `UTILITY`, mas a Meta devolveu `category: "MARKETING"` — reclassificação automática, e o próprio código já reconhecia isso em outro lugar (`sendComeBackMessage`, `reminders.ts`: "Mensagem de reconquista é categoria MARKETING na Meta") sem que `templates.ts` refletisse.
  - **Corrigido** (`templates.ts`/`whatsappConnect.service.ts`): `TemplateDefinition` ganhou `example: string[]` (valor de exemplo por variável, só usado na revisão) e `category` passou a aceitar `"MARKETING"`; `come_back_message` corrigido pra `MARKETING`; `createTemplates` manda `example: { body_text: [tpl.example] }` no payload. Vale pra qualquer barbearia nova que conectar a partir de agora, não só a Vintage.
  - **Reenviado pra Vintage**: apagados os 3 templates rejeitados (`DELETE .../message_templates?name=...`) e recriados com o body corrigido. **Achado no processo**: depois de deletar, a Meta trava a recriação do mesmo nome com `error_subcode 2388023` ("Message template language is being deleted") por bem mais tempo do que a mensagem sugere ("try again in less than 1 minute") — na prática levou mais de 15min pra liberar, mesmo com a listagem via GET já mostrando vazio antes disso. Se precisar repetir esse padrão (apagar+recriar template) no futuro, esperar uns 15-20min antes de insistir, não só 1min.
  - **Status atual**: os 3 templates estão `PENDING`/`rejected_reason: NONE` na Meta (passaram na validação de formato, agora na fila normal de revisão) — falta só a aprovação chegar via webhook (`handleTemplateStatusUpdate`). ~~`whatsappConnectionStatus` da Vintage no banco já reflete `pending_templates` corretamente~~ **não mais válido em 10/09** — ver seção da sessão seguinte, virou `disconnected` por um bug à parte.

### Sessão de 10/09: bug real no token de mensagem manual marcava a Vintage como desconectada por engano

- **Sintoma**: checando ao vivo (`GET .../message_templates` direto na Graph API, fora do código), `Business.whatsappConnectionStatus` da Vintage estava `"disconnected"` — não `pending_templates` como o CLAUDE.md dizia. Usuário confirmou que o WhatsApp Business App no celular dele mostrava conectado normalmente, e que o bot automático continuava respondendo — ou seja, o status no banco estava errado, não a conexão real.
- **Causa raiz**: `sendManualMessage` (`chatEngine.ts`, mensagem manual do dono pela aba Conversas → Mensagens) chamava `sendWhatsappText(phoneNumberId, phone, text)` **sem passar o `accessToken` da barbearia** — só 3 argumentos, faltando o 4º. Caía pro token global da plataforma, que não tem permissão sobre o `phoneNumberId` de uma barbearia com WhatsApp próprio conectado (WABA diferente). O erro de permissão da Meta batia com a assinatura de "conexão caiu de verdade" (`isWhatsappDisconnectionError`, código 190 ou 100/subcode 33) e `markWhatsappDisconnectedIfNeeded` marcava a barbearia como `disconnected` — um falso positivo causado pelo próprio bug, não uma queda real. `sendManualAttachment`, a função irmã logo abaixo no mesmo arquivo, já fazia certo (`resolveBarbershopAccessToken`) — só a de texto tinha esquecido.
- **Corrigido** (`chatEngine.ts`): `sendManualMessage` agora passa `resolveBarbershopAccessToken(barbershop)` também.
- **Achado secundário durante a investigação, corrigido junto**: uma vez marcada `disconnected`, nada no sistema revertia o status sozinho depois — só reconectar manualmente pelo painel (desnecessariamente disruptivo pra uma barbearia cujo WhatsApp nunca tinha caído de verdade). Usuário pediu explicitamente que isso se corrigisse sozinho. Adicionada a contrapartida `markWhatsappReconnectedIfNeeded` (`whatsappConnect.service.ts`) — chamada depois de qualquer envio real bem-sucedido (bot automático no webhook, mensagem/anexo manual do painel, lembrete/reagendamento/reconquista do cron, OTP), reverte `disconnected`→`connected` só se o status atual for exatamente `disconnected` (prova direta de que a conexão funciona); não mexe se já estava noutro status (`pending_templates` etc.) — o próximo evento de aprovação/rejeição de template via webhook continua decidindo entre `connected`/`error` normalmente depois disso.
- Testes novos: `whatsappConnect.service.test.ts` (4 casos cobrindo as duas funções contra o banco real) + `chat.routes.test.ts` (regressão específica do bug: token próprio é passado quando a barbearia tem WhatsApp conectado).
- **A correção da Vintage no banco é automática**: assim que o próximo envio real (bot respondendo um cliente, por exemplo) for bem-sucedido, `markWhatsappReconnectedIfNeeded` já reverte sozinha pra `connected` — decisão consciente de não editar o campo direto em produção, o usuário preferiu deixar a correção acontecer pelo próprio fluxo normal.
- Ainda não portado pro `odonto-saas` — o mesmo bug (token faltando em `sendManualMessage`) provavelmente existe lá também, conferir. Deixada tarefa sugerida (spawn_task) pra isso.

### Checklist de onboarding "Primeiros passos" (10/09)

QA de 10/09 apontou (achado #4) que a conta nova não tinha como saber o que já estava pronto vs. o que faltava — o tour guiado existente é passivo (mostra uma vez, não acompanha progresso), diferente de um checklist. Implementado:

- `getOnboardingChecklist(businessId, userId)` (`onboarding.service.ts`) — 4 itens, todos derivados do que já existe no banco, sem estado próprio: **Conectar WhatsApp** (`whatsappConnectionStatus !== "not_connected"`), **Confira os serviços** (mais de 1 serviço, OU o único serviço já foi renomeado do nome padrão do signup — evita ficar "incompleto" pra sempre pra quem só precisa de 1 serviço), **Criar primeiro agendamento** (`Appointment` count > 0), **Confirmar e-mail** (conta sem e-mail cadastrado, como as de seed/demo, conta como feito automaticamente).
- Rota `GET /api/manage/onboarding-checklist` (`requireOwner`).
- `admin.html`: card "Primeiros passos" na Visão Geral, consultado a cada `loadAll()`, escondido sozinho quando `complete` vira `true`. Cada item é clicável e navega direto pra onde a ação acontece (Configurações → WhatsApp com scroll até a seção, Serviços, Agenda) — reaproveita o clique nas abas de `#page-tabs`, sem rota nova.
- Testes: 3 casos novos em `onboarding.service.test.ts` (conta nova sem nada feito, e-mail null conta como feito, todos os 4 itens completos via edição direta no banco).
- Não portado pro `odonto-saas` ainda.

### Pendências reais

1. ~~Reescrita do frontend~~ **RESOLVIDO (10/09)** pros dois componentes que faltavam — `.plan-card`/`.tipo-card` portados do `odonto-saas` (ver seção da sessão acima). `webroot/*.html` continua HTML/JS puro (não framework) por decisão de arquitetura, isso não muda. **`barber.html` já está majoritariamente tokenizado** (checado em 08/09) — não é mais dívida de cor, no máximo composição visual das páginas internas.
2. Exclusão LGPD é global entre barbearias (por `Client` ser entidade compartilhada por telefone) — decisão de arquitetura documentada, não bug; revisitar se virar problema real.
3. Confirmar entrega do OTP com o número real de produção da Vintage (o teste de 15/08 usou número de teste da Meta sem verificação de negócio completa — ver bullet do WhatsApp acima) — não bloqueia nada, só falta fechar a confirmação. **O número de teste da Meta usado nesse teste (`+1 555-310-6678`) passou a ser usado pra validar outro projeto** (20/08) — sem impacto aqui, a Vintage está `not_connected` no banco em relação a ele. `sendWhatsappAuthTemplate` (`src/lib/whatsapp.ts`) ganhou um `console.log` de sucesso (08/09) — antes só logava erro, "não deu erro" era a única evidência de entrega. **Causa do bloqueio resolvida (09/09)** — ver seção da sessão acima: os templates da Vintage foram corrigidos e reenviados, estão `PENDING` na Meta. Ainda falta a aprovação chegar e então testar OTP de verdade (com o próprio número do dono, nunca conta de cliente real).
4. Reenvio do App Review (`business_management`) ainda não feito. **Decisão (08/09): não reenviar por enquanto** — levantamento no código confirma que nada depende dessa permissão (só `whatsapp_business_management`, já aprovada, é checada em algum lugar); Embedded Signup e Coexistence já funcionaram ponta a ponta sem ela. Reavaliar só se alguma feature futura exigir a Business Management API de verdade.
5. ~~Cobertura de testes ainda não fechada~~ **RESOLVIDO (28/08)**: ~~`/api/chat` (chat.routes.ts) como rota HTTP~~ — `src/modules/chat/chat.routes.test.ts` novo (16 testes: POST /api/chat, /api/chat/reset, e as rotas autenticadas de Conversas do painel — listagem, transcrição, toggle de IA Ativa, envio manual de mensagem/anexo). ~~Stripe Connect/webhooks de `clientPlans` fim a fim~~ — `src/modules/clientPlans/clientPlans.routes.test.ts` novo (6 testes no `POST /api/webhooks/stripe-connect`: assinatura inválida, `account.updated`, `checkout.session.completed`, `customer.subscription.updated` incluindo o reset de cota só na renovação real, `customer.subscription.deleted`, evento desconhecido; assina o payload localmente com `Stripe.webhooks.generateTestHeaderString`, sem chamada de rede — eventos v2 "thin" não cobertos). ~~`campaigns`, `googleCalendar`~~ — `src/modules/campaigns/campaigns.routes.test.ts` (4 testes: quem entra no alvo de reativação, quem recebe de fato dentre os alvos, contadores de skip) e `src/modules/googleCalendar/googleCalendar.test.ts` (18 testes: service + rotas, isolamento entre tenants, retry automático em token expirado — mocka `src/lib/googleCalendar.ts` inteiro, já que dev/teste não tem `GOOGLE_CLIENT_ID`/`SECRET` configurado). Suíte completa: 25 arquivos, 157 testes.
6. ~~Google Agenda~~ **RESOLVIDO (23/08)** — publicação em produção (20/08) + fix do fire-and-forget em serverless (23/08, ver acima). Confirmado ponta a ponta: evento real criado no Google Agenda do barbeiro conectado.
7. ~~Vale revisar se outras chamadas fire-and-forget...~~ **RESOLVIDO (28/08)** — auditados todos os `.catch()` do repo. Achado e corrigido mais um caso real: `logChatUsage` (`src/modules/chat/chatUsage.ts`), chamado sem `await` dentro do loop principal do bot (`chatEngine.ts`) — mesmo risco do bug do Google Agenda, mas de impacto menor (só perdia telemetria de custo em `ChatUsageLog`, não a funcionalidade). Os demais `.catch()` do repo são `await res.text().catch(() => "")` (leitura de corpo de erro, já aguardados) ou entry points de cron/boot (`jobs/backup.ts`, `jobs/reminders.ts`), fora do caminho de uma resposta HTTP em andamento — sem o mesmo risco. Commit `0ca2633`.
8. Depois de qualquer push, conferir se o deployment novo foi promovido pra produção automaticamente (ver nota "Convenções operacionais" acima) — aconteceu de novo em 23/08, 04/09 e **09/09** (esse último: TODOS os commits da sessão de 08-09/09, do Fase 1 até o fix dos templates, ficaram só em Preview por ~19h até a promoção manual via `vercel promote` — ou seja, o fix que impede template mal formado em barbearia nova só passou a valer de verdade depois dessa promoção, não no momento do push).
9. ~~Templates da Vintage rejeitados pela Meta na WABA nova de Coexistence~~ **RESOLVIDO (09/09)** — causa raiz achada (faltava `example` obrigatório + categoria errada em `come_back_message`, ver seção da sessão acima), corrigido no código e reenviado. Os 3 estão `PENDING` na Meta agora; falta só a aprovação chegar via webhook pra lembrete/reagendamento/reconquista/OTP automáticos voltarem a funcionar nessa conexão.

### Sessão de 11/09: debounce de resposta do bot, transcrição de áudio e anexo de áudio/vídeo em Conversas

Pedido do usuário: (1) esperar ~20s antes da IA responder, pra juntar mensagens mandadas em sequência rápida numa resposta só; (2) o bot entender mensagem de voz (transcrever e responder); (3) a aba Conversas → Mensagens poder mandar áudio/vídeo/foto pro cliente, não só imagem/PDF.

- **Debounce de 20s (`chatEngine.ts`, `whatsapp.routes.ts`)**: `sendMessage` (usado até agora pelo webhook) foi dividido em `recordIncomingMessage` (grava a mensagem do cliente + timestamp `lastCustomerMessageAt`, decide se o caminho é imediato — bloqueio de cobrança/IA pausada, sem IA — ou "queued") e `generateReplyFromHistory` (chama a IA e manda a resposta, lendo o que já foi gravado). O webhook chama os dois separados, com uma espera de `WHATSAPP_REPLY_DEBOUNCE_MS` (20s por padrão, `src/config/env.ts`) entre eles: a cada `WHATSAPP_REPLY_DEBOUNCE_POLL_MS` (3s) verifica via `hasNewerCustomerMessage` se uma mensagem mais nova chegou nesse meio tempo — se sim, essa invocação desiste sem responder (a invocação da mensagem mais nova assume, e só ela efetivamente espera os 20s inteiros). `sendMessage` continua existindo como wrapper de `recordIncomingMessage` + `generateReplyFromHistory` sem espera nenhuma — usado pelo simulador (`chat.html` → `POST /api/chat`) e pelos testes que não devem esperar 20s reais. **Decisão consciente de arquitetura**: o debounce roda FORA de qualquer transação de banco — a versão antiga de `sendMessage` segurava um lock de linha (`FOR UPDATE`) durante toda a chamada de IA; segurar esse lock por 20s adicionais seria arriscado (contenção, timeout do pooler do Supabase). Novo campo `ChatSession.lastCustomerMessageAt` (migration `20260911000000_chat_session_last_customer_message_at`), separado de `updatedAt` de propósito (que também muda em saves do dono/IA, confundiria o sinal). `vercel.json` ganhou `functions.maxDuration: 60` (era o default da Vercel, insuficiente pra 20s de espera + geração da IA) — **plano Hobby permite até 60s configurável**, mas vale medir na prática; se a geração da IA por si só já se aproximar desse teto em casos reais (tool loop longo), pode precisar do plano Pro (300s) — mesma pendência já registrada de "deveria subir pro Pro".
- **Transcrição de áudio (`src/lib/transcription.ts`, novo)**: mensagem de voz recebida é baixada da Meta (`downloadWhatsappMedia`, novo em `src/lib/whatsapp.ts` — caminho inverso de `uploadWhatsappMedia`) e transcrita via **Groq** (roda o Whisper na própria infra deles, `whisper-large-v3-turbo`) — escolhido depois de pesquisa comparativa (ver sources abaixo) por ter cota gratuita **permanente** (2.000 req/dia, 8h de áudio/dia, sem cartão) que cobre o volume real de uma barbearia, diferente de Deepgram (só US$200 de crédito único, expira) ou Google Cloud STT (60min/mês). A Claude API não recebe áudio diretamente (só texto/imagem/PDF), daí precisar desse passo à parte. Texto transcrito entra no histórico prefixado com `[Áudio transcrito] ` (transparência pro dono na aba Conversas + contexto pra IA). Se a transcrição falhar (sem `GROQ_API_KEY` configurada, erro de rede, áudio incompreensível) o bot avisa e pede pra tentar de novo ou escrever, em vez de travar silenciosamente. **`GROQ_API_KEY` ainda NÃO configurada em produção** — sem ela, `transcriptionConfigured` é `false` e áudio recebido cai no aviso antigo ("só consigo entender texto"), sem quebrar nada; falta o usuário criar a conta em console.groq.com e configurar a env var na Vercel pra transcrição funcionar de verdade.
- **Anexo de áudio/vídeo em Conversas (`admin.html`, `src/lib/whatsapp.ts`, `chat.routes.ts`)**: `sendWhatsappMedia` agora classifica o mime type em `image`/`video`/`audio`/`document` (antes só `image`/`document` — vídeo e áudio mandados manualmente viravam "documento" genérico, sem preview/player nativo no WhatsApp do cliente). Input de arquivo (`accept`) e limite de tamanho (10MB → 16MB, o teto de áudio/vídeo da própria Cloud API) atualizados; `express.json()` de `app.ts` subiu de 15mb pra 25mb (o corpo é base64, ~33% maior que o arquivo — 16MB\*1.34≈21.4MB, precisa de folga).
- Testes novos: `src/modules/chat/debounce.test.ts` (`recordIncomingMessage`/`hasNewerCustomerMessage` contra o banco real), `src/lib/transcription.test.ts` (Groq mockado — sucesso, erro HTTP, texto vazio, falha de rede), testes novos em `src/lib/whatsapp.test.ts` (`downloadWhatsappMedia`, classificação de mime type em `sendWhatsappMedia`), `whatsapp.routes.test.ts` reescrito pro novo fluxo (mocka `recordIncomingMessage`/`generateReplyFromHistory`/`hasNewerCustomerMessage`/`transcribeAudio` em vez de `sendMessage`, com `WHATSAPP_REPLY_DEBOUNCE_MS`/`POLL_MS` setados baixos só nesse arquivo de teste pra não esperar 20s reais). Suíte: 40 arquivos, 256 testes, `tsc --noEmit` limpo.
- **Não portado pro `odonto-saas` ainda** — só depois de mais uso real aqui, mesmo padrão de sempre.
- **Pendências reais desta sessão**: (a) configurar `GROQ_API_KEY` na Vercel pra transcrição funcionar de verdade; (b) validar ponta a ponta com WhatsApp real — nenhuma das duas partes (debounce, transcrição) foi testada contra o WhatsApp de verdade ainda, só a suíte automatizada com tudo mockado; (c) medir na prática se `maxDuration: 60` é suficiente (debounce de 20s + geração da IA, que já tinha timeout de transação de 30s antes disso) ou se algum caso real de tool loop longo estoura o teto.

Sources sobre transcrição gratuita: [Groq pricing 2026 — eesel AI](https://www.eesel.ai/blog/groq-pricing) | [Groq API Free Tier Limits — Grizzly Peak Software](https://www.grizzlypeaksoftware.com/articles/p/groq-api-free-tier-limits-in-2026-what-you-actually-get-uwysd6mb) | [Free Speech-to-Text APIs 2026 — Spokenly](https://spokenly.app/blog/free-speech-to-text-apis)

## Login de demonstração

Senha `barbearia123` para todos. Dono: `barbearia-vintage.dono` (3 barbeiros — `carlos`, `rafael`, `diego`) ou `barbearia-solo.dono` (barbeiro único — `marcos`, pra testar o modo barbeiro-único vs múltiplos; criada por `scripts/seed-solo-barbershop.ts`, seguro rodar de novo).

A barbearia `barber-king.dono` (barbeiros `lucas`/`bruno`, criada pelo `prisma/seed.ts` original) ainda existe no banco e continua logável, só não aparece mais na tela de login nem é mais a credencial de demonstração oficial.

Super-admin: usuário `admin`, senha configurada via `ADMIN_PASSWORD_HASH` no `.env`/Vercel (gerar com `npx tsx scripts/hash-password.ts <senha>`).
